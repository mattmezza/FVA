<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" >

    <!--
         A 'parent' preference, which enables/disables child preferences (below)
         when checked/unchecked.
    -->
    <CheckBoxPreference
        android:defaultValue="true"
        android:key="extr_algo_use_sift"
        android:title="@string/pref_title_use_sift" android:summary="@string/pref_desc_use_sift"/>

    <!-- Allows the user to choose a ringtone in the 'notification' category. -->
    <!-- NOTE: This preference will be enabled only when the checkbox above is checked. -->
    <!-- NOTE: RingtonePreference's summary should be set to its value by the activity code. --><CheckBoxPreference
        android:defaultValue="true"
        android:key="extr_algo_use_lbp"
        android:title="@string/pref_title_use_lbp" android:summary="@string/pref_desc_use_fbp"/>

    <!-- NOTE: This preference will be enabled only when the checkbox above is checked. -->

</PreferenceScreen>
